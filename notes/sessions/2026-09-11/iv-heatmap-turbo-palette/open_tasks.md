# Open Tasks — iv-heatmap-turbo-palette

## Active

- [ ] **[待 KAI 定] 0 值是否要退回中性色** —— 换成 Turbo 后，ΔIV ≈ 0 的格子
      显示为亮黄绿（`#a4fc3b`），整块面板偏亮；原方案 0 = 中性暗灰 `#1a1f26`，
      异常格更跳。两个选项：
      - (a) **维持 Turbo**（当前状态，忠实于参考项目截图 + "只改色调"指令）；
      - (b) 若 KAI 想要"0 中性、两端发散"，可换用参考项目 *change* 热力图的
        `RdBu_r`（发散、中性接近浅灰）—— 但这**不是**截图里那张图的色调，
        且已超出本次授权，需 KAI 明确指示后再动。
      - 注：方案 (b) 里"0 中性"仍取决于色板中位色；`RdBu_r` 中位是浅灰白
        （`#f7f7f7`），在深色主题面板上会**很亮**，需一并评估。

- [ ] **[低] 热力图图例文字与新色阶的语义一致性复核** —— `visualMap.text`
      仍是 `["IV 上行", "IV 下行"]`。Turbo 下高值=红（暖）、低值=紫蓝（冷），
      与"上行/下行"仍然对得上，**暂无需改**；列出仅为留痕。

## Closed（本会话）

- [x] **把前端 IV 热力图色调改为参考项目的 `Turbo`** —— 2026-09-11：
      `web/config.js::heatmap.palette` 11 色发散 → 15 色 Turbo 顺序色阶；
      只改这一个数组 + 其注释。验证：`node --check` / `run.py --check` 11/11 /
      `check_web_contract` 全过 / `check_page_render` 全过（34 档 × 74 桶 ·
      544 格 · 色标 ±0.64）/ 线上 `config.js` 已确认新色 / 无头 Chrome 实拍归档。
