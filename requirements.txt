# 0DTE IV Skew 与日内动能监控系统 —— 运行时依赖
#
# 安装：
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   source .venv/bin/activate       (macOS / Linux)
#   pip install -r requirements.txt
#
# 说明：
#   * 数值计算全部用标准库完成（statistics / math / collections.deque），
#     不引入 numpy / pandas —— 减少依赖面，也让每层都能被单独读懂。
#   * tzdata 在 Windows 上是必需的：Windows 自带时区数据库，Python 的
#     zoneinfo 找不到 "America/New_York" 会直接抛 ZoneInfoNotFoundError。

ib_async>=2.1.0,<3.0
aiohttp>=3.9,<4.0
tzdata>=2024.1
