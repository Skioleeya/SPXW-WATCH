{
  "_comment": "L6 组装层 — 流水线节奏与生命周期。compute_interval_ms 决定特征计算频率，应不大于 transport.push_interval_ms。裁剪节奏不在这里：它属于状态层，见 state.prune_interval_s。",
  "compute_interval_ms": 400,
  "log_stats_every_s": 60.0,
  "startup_grace_s": 1.0,
  "shutdown_timeout_s": 10.0,
  "reset_feature_state_on_reconnect": false
}
