/* 前端展示配置
 * ------------------------------------------------------------------
 * 这里只放【呈现】参数。任何与后端有关的取值（端口、WS 路径、推送频率、
 * 矩阵尺寸）一律不在这里硬编码，而是启动时从 /health 动态获取。
 *
 * 理由：端口和路径属于后端的 config/transport.json，前端复制一份就等于
 * 制造了"两份真相"，改一处忘一处必然导致连不上。
 * ------------------------------------------------------------------ */

window.SWATCH_CONFIG = {
  /* 断线重连退避 */
  reconnect: {
    initialMs: 400,
    maxMs: 8000,
    factor: 1.7,
    jitterMs: 200
  },

  /* 渲染节流：后端推送可能远快于屏幕刷新，按帧合并 */
  render: {
    maxFps: 5,
    staleWarnMs: 15000,
    /* 超过这个时长没有**成功渲染过**的帧 = 数据中断。取值必须远大于后端
       push_loop 的心跳周期（config/transport.json 的 heartbeat_interval_s = 20s：
       行情不动时后端也会每 20 秒补推一帧）—— 45s 不会把"午后死水"误判成中断。 */
    staleErrorMs: 45000,
    /* 数据中断时**是否真的换连接**，还是只把状态文字改成"数据中断 Ns"。
       默认 true。留这个开关是为了能对同一份代码做非空转 A/B：
       翻成 false ⇒ 行为回到 2026-09-17 修复之前（只报不改，页面永不自愈）。 */
    reconnectOnStale: true
  },

  /* 热力图 */
  heatmap: {
    /* 色标：Plotly `Turbo` 顺序色阶，逐色抄自参考项目 live-volatility-surface
       （dash_surface.py 的 IV_COLORSCALE，其 DASHBOARD_CONFIG_DEFAULTS 取 "Turbo"，
       未反转）。冷端 = IV 下行，暖端 = IV 上行。
       取位规则：ECharts 的 inRange.color 数组与 Plotly 的 list 色阶一致，都是在
       数组上等距取点，故 15 色落在 0, 1/14, …, 1 —— 与本图 visualMap 的
       [-vmax, +vmax] 对齐后，两端读数与原项目同色。
       注意：这是顺序色阶，不是发散色阶；0 值落在第 8 色（黄绿），不再是中性灰。 */
    palette: [
      "#30123b", "#4145ab", "#4675ed", "#39a2fc", "#1bcfd4",
      "#24eca6", "#61fc6c", "#a4fc3b", "#d1e834", "#f3c63a",
      "#fe9b2d", "#f36315", "#d93806", "#b11901", "#7a0402"
    ],
    /* 防除零守卫：色标量程一律采用后端下发的 vmax（后端已按配置的分位截断 +
       下限保护算好）。这里只在 vmax 缺失或为 0 时兜一个极小值，避免色标
       min==max 导致映射出 NaN 颜色。它不是"色标下限策略"，不要在这里调节
       对比度——那是 config/serialization.json 的事。 */
    boundEpsilon: 0.05,
    /* 纵轴行权价标签最多显示多少个，避免拥挤 */
    maxYLabels: 26,
    /* 横轴时间标签间隔（按列数自动调整的基准） */
    xLabelCount: 14,
    showSpotLine: true,

    /* 网格线 —— 旧版 ECharts 的 `itemStyle: {borderWidth:1, borderColor: theme.grid}`
       在 af2e2e4 的 WebGL 重写中被删除（theme.grid 一度成为**死配置**，2026-09-14 定位）。
       2026-09-14 渲染器换回 ECharts 后，恢复方式改为轴 `splitLine`：
       颜色取 `theme.grid`、宽度 1px，`cellBorderMix` 映射为线的 opacity ——
       `opacity m` 的线叠在数据色上 ≡ 旧 shader 的 `mix(color, border, m)`，数学等价。

       **网格线按最小间距抽样**：细档位列宽只有 1–2 像素（30 秒档 2370 列在
       1250px 网格里每列仅 0.53px；1 分档 630 列 ⇒ 2px），每格都画线等于把格子吃光。
       所以每 `stride` 个分类画一条，`stride = ceil(cellBorderMinPx / cellPx)` ——
       线间距恒 ≥ 本值。
       用 splitLine 而不是逐格 `itemStyle.borderWidth` 的原因：后者要给 5.7 万个矩形
       各描一次边，在 2370 列下既是性能灾难、又会把细档位糊成一片。

       取值说明（2026-09-14 由 5 调到 12，KAI 定的方案 A）：
       原值 5 在细档位下**等于没画** —— 30 秒档 1086 列 ⇒ cellPx=1.02px，
       stride=ceil(5/1.02)=5 ⇒ 线间距仅 5.12px。1px 宽、55% 不透明的深色线落在
       同样 1px 宽的彩色数据格上，被数据色吃掉；实测截图里**空白区网格清晰、
       数据密集区完全看不见**。且 `stride × cellPx` 在 cellPx < 本值时会锁死在
       本值附近（ceil 的必然结果）⇒ "无极调节"在稠密区实际失效。
       调到 12 后线间距恒 ≈ 12.3px，30 秒档线数 217 → 90，密集区可辨。
       副作用：5 分档 cellPx=10.2px < 12 ⇒ stride 由 1 变 2，间距 10.2 → 20.4px，
       网格变稀（仍够用，15 分档 cellPx=30px 不受影响，stride 仍为 1）。

       取值边界：太小（< 3）线会挨到一起变糊；太大（> 20）网格会稀到失去"格子"
       的观感、细档位的格子被抹平。单位是 CSS 像素（ECharts 坐标单位）。 */
    cellBorderMix: 0.55,
    cellBorderMinPx: 12,

    /* 成交量 Top-N 高亮 —— 对**整个可视区域**（本图即整张矩阵）内成交量排名
       前 `topN` 的格子描黑边，其余格子完全不描边（普通填充）。

       语义边界（2026-09-15 KAI 定）：
       - **不是**逐格按成交量无极变粗 —— 那会让满屏都是黑边、反而看不出谁最活跃。
       - **只**给 Top-N 描边，`topN` 之外的格子一律无黑边。这是"标记"不是"连续编码"。
       - 描边宽度**无极调节**：按"该格成交量 / 第 topN 名的成交量"线性插值，
         落在 `minPx … maxPx`（CSS px）之间 —— 同属 Top-N 的格子也分得出强弱，
         第 1 名最粗、第 topN 名最细。

       数据源唯一：**30 秒桶的 tick 计数**（后端 `vol_bm`/`vol_i16`，scale=1）。
       更粗的显示周期由 `web/period.js::aggregate` 在组内**求和**得到，不另取数据。

       为什么是黑色：旧 WebGL shader 是 `mix(color, white, 0.6)`，白边压在高值区的
       亮黄绿填充上几乎看不出边界；黑色在整条 Turbo 色阶上都对比明确。

       排序口径：成交量相同则按"先列后行"稳定排序（同值不被随机打散，
       否则每帧高亮会跳动）。 */
    volumeTop: {
      enabled: true,
      /* 描多少格。0 或负数 = 关闭高亮（所有格子都不描边）。 */
      topN: 3,
      /* 边框颜色，所有高亮格统一。 */
      color: "#000000",
      /* 描边宽度区间（CSS px）。未显式指定时按格子短边派生，见下。 */
      minPx: null,
      maxPx: null,
      /* 未指定 minPx / maxPx 时的派生比例：宽度 = 格子**短边** × 本值。
         锚短边是因为锚长边时窄行的上下边框会互相吃穿。 */
      maxRatio: 0.30,
      minRatio: 0.10
    },

    /* 横轴（时间桶）滚轮缩放 —— **本图自己的**交互，与 Skew 面板无关。
       2026-09-17 KAI 裁定：取消两个面板之间的滚轮联动。此前横轴的裁剪由 Skew
       的缩放经 `state.viewport` 驱动（在热力图上滚轮只缩 Skew、热力图跟着被裁），
       现在两边各管各的：滚轮在**热力图**上 = 左右缩放时间轴；Skew 面板照常
       自动滚动，两者互不影响。

       窗口按**列下标**保存（`web/heatmap.js::_captureX`），不按百分比：
       横轴每帧往尾部追加新列，百分比窗口会随列数漂移 —— 用户盯着的
       那段时间会自己跑掉。 */
    xZoom: {
      enabled: true,
      /* 窗口最少保留的列数（映射到 ECharts `dataZoom.minValueSpan`）。
         再缩就只剩几列，看不出时间结构。 */
      minCols: 8,
      panOnDrag: true,
      panThrottleMs: 100,
      /* "横向拖了这么多像素才算一次意图明确的拖动"。全宽时横向拖是**空操作**
         （见 heatmap.js 文件头），超过这个阈值就报一声提示 —— 不报的话
         用户会把它读成"图坏了"。设太小会把单击也判成拖动。 */
      hintPx: 4
    },

    /* 横轴「自动滚动」(auto roll)：窗口右缘钉在最新列上，列一追加就跟着走。
       策略（触发条件 / 方向 / 速度 / 何时松手）与理由全在 `web/heatmap_roll.js`，
       这里只放两个量。

       为什么要有它：窗口存的是**列下标**，列每帧往尾部追加，下标窗口原地不动
       ⇒ 缩进去看细节之后，图会越看越旧、最后停在历史里。全宽没这个问题
       （全宽天然含最新列），所以自动滚动只在**有窗口**时才有意义。 */
    xRoll: {
      enabled: true,
      /* 速度的**唯一**调节量：右缘距最新列留几列。
         0 = 右缘就是最新列（默认，"零滞后"）；>0 = 固定滞后若干列，
         用于"想看已经落定的桶、不想被半根新柱牵着走"。
         不设"每秒滚几列"这类定时速度 —— 那是**插值动画**，见 heatmap_roll.js
         文件头：一次 setOption 实测 p50 27.4ms，插到 60fps 做不到。 */
      lagCols: 0
    },

    /* 指针反馈（十字线 + 命中格描边）。由 `web/heatmap_hover.js` 用 DOM 覆盖层
       实现，**不是** ECharts 的 emphasis / axisPointer —— 实测两者在这张图上
       都留不住（悬停态活不过 400ms 的一次重建；原生 axisPointer 三种写法
       一条线都不画），理由与证据见该文件头。 */
    hover: {
      enabled: true,
      crosshair: true,
      /* 命中格描边的线宽（CSS px）。 */
      cellBorderPx: 1.5
    },

    /* 可选时间周期（秒）。这是纯呈现参数：它只决定给用户几个缩放档位。
       真正的基线桶宽由后端帧里的 heatmap.bucket_seconds 给出，前端不复制 ——
       不满足"基线桶宽整数倍"的项会被自动丢弃并告警（见 web/period.js）。 */
    periods: [
      { seconds: 30, label: "30秒" },
      { seconds: 60, label: "1分" },
      { seconds: 180, label: "3分" },
      { seconds: 300, label: "5分" },
      { seconds: 900, label: "15分" }
    ],
    /* 打开页面时默认选中的周期（秒）。不在可用列表里则回退到列表第一项。 */
    defaultPeriodSeconds: 60,
    /* 一屏最多画多少列，超出只画**最近**的若干列。
       取值必须 ≥ 整个交易日网格的桶数（2370 = 71100s ÷ 30s），也就是说：
       **在一个交易日之内这个截断永远不会生效**。这不是"忘了调小"，是刻意的。
       横轴是时间轴，从尾部截掉历史段在图上**看不出来**（只是左边少了几列、
       没有滚动条也没有提示），于是 GTH 开盘那一段会静默消失 —— 而"能在 GTH
       时段完成实盘验证"恰恰要求看得见 GTH。密度交给周期选择器：30 秒全时段
       是 2360 列（很密），想看全貌就切 5 分钟。
       tools/check_period_aggregation.py 的跨文件不变量会核对这个下限。 */
    maxColumns: 2370
  },

  /* 时段视图。
     按钮本身**不在这里声明** —— 时段真相在 config/app.json 的 sessions，
     随帧下发到 session.zones（含 id 与显示名），前端照着生成按钮。在这里抄一份
     "GTH / RTH" 就是把会话定义抄了第二遍，后端加一个时段前端不会跟着变。
     这里只放那个**合成的**"全部时段"档的显示名。 */
  sessions: {
    allId: "all",
    allLabel: "全时段"
  },

  /* Skew 曲线 */
  skew: {
    showAtm: true,
    showPut25: true,
    showCall25: true,
    zeroLine: true,
    /* 纵轴留白比例 */
    padRatio: 0.18,
    /* 纵轴**刻度标签**的小数位（左右两个纵轴共用）。这**不是数据精度** ——
       数据精度是 decimals.skew / decimals.iv（提示框与顶栏读数用）。刻度只需
       够分辨量级，位数给多了反而糊成一团。 */
    axisDecimals: 1,
    /* 横轴时间标签的目标数量。按**当前可见列数**算间隔，所以滚轮放大之后
       标签会自动变密（与 heatmap.xLabelCount 同一规则）。 */
    xLabelCount: 14,

    /* 缩放 / 平移（**全部由鼠标拖动驱动**，唯一入口在 `web/skew_zoom.js`）。
       ------------------------------------------------------------------
       为什么需要它：Y 轴量程默认由 `axisRange()` 每帧按"可视窗口内极值 + 留白"
       自动重算。Skew 长期在 2–3 波动率点内小幅波动，自动量程会把这点波动
       拉满整个面板高度 —— 看不出形态；而量程一被拉开（比如开盘跳空），
       小波动又被压成一条直线。

       触发方式（2026-09-17 KAI 改版；**滚轮缩放整条删除**）：
         · 左 Y 轴区上下拖 → 只缩 **25Δ Skew**（左轴）
         · 右 Y 轴区上下拖 → 只缩 **IV**（右轴）
         · 底部 X 轴区左右拖 → 缩 / 放**时间窗**
         · 网格内按住拖 → 自由平移（上下 = 两条纵轴量程一起移；左右 = 挪时间窗，
           **但全宽时横向拖不动** —— 全宽表示"自动跟随最新"，凭空长窗会让它静默停止跟随）
         · 双击 → 三条轴全部复位
       归属在**按下那一刻**按位置定死，整段拖动不再改判 —— 中途改判会让同一次
       拖动一半在缩、一半在移（用户看到"图在抖"），两条纵轴还会只锁一半。
       方向统一为「**往轴的正方向拖 = 放大**」：Y 轴区向上 = 放大，X 轴区向右 = 放大。

       ⚠️ 缩放 / 平移一旦建立就**锁定**（不再被自动量程覆盖），双击复位。
       Y 轴区拖动只锁**那一侧**（KAI 2026-09-17 裁定）；网格内上下平移则
       **两条一起进锁定态** —— 两条轴共用同一段屏幕高度，只锁一条的话，
       另一条下一帧按新数据自己重算，两条曲线就会"各走各的"。
       ------------------------------------------------------------------ */
    zoom: {
      enabled: true,
      /* 拖动**每级**的缩放倍率。1.15 ⇒ 每 stepPx 像素约 ±15%，几级就能明显进出。 */
      step: 1.15,
      /* 每级缩放所需的拖动像素。越小越灵敏 —— 这个值同时决定
         "拖一格屏幕高度"大约能缩多少级（面板高约 220px ⇒ 约 5.5 级 ≈ 2.2×）。 */
      stepPx: 40,
      /* 时间窗最少保留的列数。再缩就只剩几列，看不出时间结构。 */
      minCols: 8,
      /* 锚点：**按下那一刻**指针所在的数值 / 列在缩放前后保持不动
         （"往哪儿拖就往哪儿缩"）。关掉则锚在量程 / 窗口中心 ——
         手感更"整"，但远离中心的位置会跑。
         ⚠️ 锚必须固定在按下位置，不能跟着当前指针走：跟着走的话，
         内容会与指针反向跑（往上拖、图往下走），手感是反的。 */
      anchorAtPointer: true,
      /* 纵轴量程的上下限（**波动率点**，两条轴共用一组）。
         两条轴的**跨度**实测相近（约 4.4 vs 5.0），而缩放作用在跨度上、
         与轴的绝对水平无关，所以无需各配一套。
         下限防止缩到数值精度以下（0.05 已远小于 axisDecimals 的 1 位小数，
         再缩就只剩一根线在动）；上限防止一次拖动把量程拉到看不出结构。 */
      minSpan: 0.05,
      maxSpan: 500
    },

    /* 拖动过程中的重绘节流。每像素都重绘会把主线程占满（实测 setOption
       p50 27ms / max 45ms），页面其它面板一起卡；松手时会补一次精确落地。 */
    drag: {
      throttleMs: 100
    },

    /* 网格内拖动平移。`enabled: false` 只关平移，轴区缩放不受影响
       （两者开关独立 —— 这是"互不冲突"的机械判据，探针按它做非空转验证）。 */
    pan: {
      enabled: true,
      /* 全宽时网格内横向拖是**空操作**（设计如此）。超过这个像素阈值就报一声
         提示，免得被读成"图坏了"。设太小会把单击也判成拖动。 */
      hintPx: 4
    },

    /* 手势区的**可见反馈**（游标语义 + 区域高亮 + 空操作提示）。
       实现见 `web/skew_regions.js`（DOM 覆盖层，理由同 heatmap.hover）。
       `enabled: false` 只关反馈，手势本身照常工作 —— 两者开关独立，
       这样"反馈没生效"与"手势没生效"在探针里能分开判。 */
    feedback: {
      enabled: true,
      regionHint: true
    },

    /* 三条 IV 曲线（右轴）的语义色。
       **必须与主序列的分段色区分开**：主序列 25Δ Skew 按正负用 hot(红)/cool(蓝)
       分段；IV 曲线若复用同样两色，图上就只剩"两条红、两条蓝"，再叠加图例里
       两个同名的 25Δ Skew，用户无法判断哪条是 IV、哪条是 Skew。三条 IV 曲线
       之间也必须互不同色。
       语义：atm = ATM 跨式（straddle）IV，put25 = 25Δ Put IV，call25 = 25Δ Call IV。
       与 style.css 的 --warn / --put / --call 同值（顶栏读数用同一套语义色）。 */
    colors: {
      atm: "#ffb020",
      put25: "#2fd07a",
      call25: "#ff5a5a"
    }
  },

  /* 小数位（仅显示用） */
  decimals: {
    spot: 1,
    iv: 2,
    skew: 2,
    impulse: 2,
    price: 2
  },

  /* 界面反馈的通用参数。
     `resetKeys` 用 `KeyboardEvent.key` 比对（大小写各列一个，不用
     `toLowerCase` —— 那样 Shift+R 与 R 会变成两套语义说不清）。
     复位是**幂等**的：没改过视图时按下去什么也不会发生。 */
  ui: {
    flashMs: 2600,
    resetKeys: ["Escape", "r", "R"]
  },

  /* 面板配色，与 style.css 的 CSS 变量保持一致 */
  theme: {
    text: "#dbe3ec",
    textDim: "#8b97a5",
    textFaint: "#5d6874",
    border: "#232b34",
    grid: "#1a2029",
    panel: "#11151a",
    accent: "#00e5ff",
    hot: "#ff5a5a",
    cool: "#4ea8ff",
    warn: "#ffb020"
  }
};
