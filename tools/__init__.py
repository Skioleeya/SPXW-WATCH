"""
tools/ —— 机械门禁与回归工具。
===============================

**不属于任何运行时层**（不在 L0–L8 的单向依赖链上）。tools/ 可以 import 任何层，
但任何层都**不得** import tools/ —— 由 ``selfcheck_structure.py`` 守住。

分工约定
--------
* ``check_*.py`` —— 回归/门禁，**能报 FAIL 就该报 FAIL**，退出码非 0。
* ``selfcheck*.py`` —— 项目级自检（分层方向、依赖白名单、行数门禁…）。
* 每个工具都必须能回答"**摘掉被守护的那行修复，它会不会红**"（非空转验证）。
  做不到的就别叫 check，叫 probe，并明确写进 docstring。
"""
