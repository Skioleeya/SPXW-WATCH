{
  "_comment": "L5 传输层 — HTTP 静态页与 WebSocket 同端口。client_queue_size=1 表示慢客户端直接丢帧（fail-closed）。ws_compression 必须为 true：它决定 WebSocket 是否协商 permessage-deflate，实测开关一次是 4.41GB/日 ↔ 0.40GB/日 的差别（11 倍），由 tools/check_ws_compression.py 守住。",
  "host": "127.0.0.1",
  "http_port": 8060,
  "ws_path": "/ws",
  "health_path": "/health",
  "static_dir": "web",
  "http_index_file": "index.html",
  "push_interval_ms": 400,
  "client_queue_size": 1,
  "max_clients": 16,
  "send_timeout_s": 1.0,
  "heartbeat_interval_s": 20.0,
  "ws_compression": true,
  "access_log": false
}
